<h1>{{post.name}}</h1>
<p>{{post.content}}</p>
<p>By {{post.User.username}} on {{post.date_created}}</p>

{{#if loggedIn}}
  <form action="/post/{{post.id}}/comment" method="post">
    <textarea name="commentContent" required placeholder="Add a comment..."></textarea>
    <button type="submit">Submit</button>
  </form>
{{else}}
  <p>Please log in to add a comment.</p>
{{/if}}

<h3>Comments:</h3>
{{#each post.Comments}}
  <div class="comment">
    <p>{{User.username}} said:</p>
    <p>{{content}}</p>
  </div>
{{/each}}