{{#if loggedIn}}
  <h1>Welcome, {{user.username}}!</h1>
{{/if}}
{{#each posts}}
  <div class="post">
    <h2>{{name}}</h2>
    <p>{{content}}</p>
    <p>By <strong>{{author.username}}</strong> on {{date_created}}</p>
    <a href="/api/post/{{id}}">Read More</a>
  </div>
{{/each}}